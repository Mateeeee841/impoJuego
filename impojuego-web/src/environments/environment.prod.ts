// Environment de producción (aplica en `ng build` / `npm run build`).
// Si el backend cambia de URL, se edita acá — única fuente de verdad.
export const environment = {
  production: true,
  apiBaseUrl: 'https://impojuego-1.onrender.com/api'
};
